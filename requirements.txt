# Materials Cartography — MP-20 test-case pipeline
# Exact versions resolved and run on Python 3.10.12 (Linux x86_64), June 2026.
# These match the stack validated in the feasibility study.
numpy==2.2.6
scipy==1.15.3
scikit-learn==1.7.2
pandas==2.3.3
matplotlib==3.10.9
pyarrow            # Parquet writer for gtm_coords.parquet (any recent version)

# domain stack
pymatgen==2025.10.7
matminer==0.9.3
ugtm==2.3.0
spglib==2.7.0

# optional, for the test runner
pytest
